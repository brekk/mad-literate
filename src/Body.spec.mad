import { assertEquals, test } from "Test"

import { caseN2, report } from "Study"

import { processBody, processBodyWithHeadmatter, processFileWithHeadmatter } from "@/Body"



test("check", () => assertEquals(true, true))

// report(processBody, "processBody", [#[[], ""]])
report(
  processFileWithHeadmatter("1.2.3"),
  "processFileWithHeadmatter",
  [#[[], "main = () => {\n\n}"]],
)
