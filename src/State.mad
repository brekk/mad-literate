import type { Maybe } from "Maybe"

import { Nothing } from "Maybe"



export alias EnsignState = {
  codeOnly :: Boolean,
  cwd :: Maybe String,
  doFormat :: Boolean,
  help :: Boolean,
  input :: Maybe String,
  madlibJson :: Maybe String,
  multiLine :: Boolean,
  output :: Maybe String,
  runOnFinish :: Boolean,
  useColor :: Boolean,
}
export alias SpiritGumState = {
  ...EnsignState,
  headmarker :: Integer,
  imports :: String,
  isLibrary :: Boolean,
  madmarker :: Integer,
}
INITIAL_STATE :: SpiritGumState
export INITIAL_STATE = {
  codeOnly: false,
  cwd: Nothing,
  doFormat: false,
  headmarker: -1,
  help: false,
  imports: "",
  input: Nothing,
  isLibrary: false,
  madlibJson: Nothing,
  madmarker: -1,
  multiLine: false,
  output: Nothing,
  runOnFinish: false,
  useColor: true,
}
